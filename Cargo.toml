[package]
name = "mp"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
arboard = "3"
regex = "1"
toml = "0.9"

[target.'cfg(target_os = "linux")'.dependencies]
arboard = { version = "3", features = ["wayland-data-control"] }

[profile.release]
lto = true
strip = true
